# 本地歌库设置
song_library_root: "/Users/z/Downloads/LOFI SONGS"

# 生成的曲库总表（会被自动覆盖）
output_catalog: "data/song_library.csv"

# 曲目使用情况记录文件
usage_log: "data/song_usage.csv"

# 支持的音频扩展名
audio_extensions:
  - .mp3
  - .wav
  - .flac
  - .m4a
  - .aac
