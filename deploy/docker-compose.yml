version: "3.9"

services:
  app:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - ../:/workspace
    command: python src/main.py
    environment:
      - APP_ENV=development
      - LOG_LEVEL=INFO
